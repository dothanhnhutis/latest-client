import React from "react";

const DetailPostModal = ({ params }: { params: { id: string } }) => {
  console.log(2);
  console.log(params.id);
  return <div>DetailPostModal</div>;
};

export default DetailPostModal;
